保持矩阵外观的 CSV 方案总结

核心思想：每检测到一组（3个4×4矩阵），不展平为一行，而是以矩阵原有的4行×4列形式写入CSV文本，组间用空行分隔，这样文件可直接用文本编辑器查看，每个矩阵清晰可见。

---

📝 写入代码

```python
import numpy as np

class MatrixCsvWriter:
    def __init__(self, filepath):
        self.file = open(filepath, 'w')
        # 可选：写一个简单头部注释
        self.file.write("# 每组由三个4x4矩阵组成，矩阵间用空行分隔\n")

    def write(self, m1, m2, m3):
        """写入一组三个4x4矩阵（均为numpy array，dtype=float64）"""
        for mat in (m1, m2, m3):
            # 将矩阵按 CSV 格式写入4行（每行4个数字，逗号分隔）
            np.savetxt(self.file, mat, fmt='%.6f', delimiter=',')
            self.file.write('\n')   # 矩阵后面加空行分隔
        # 可选：定期刷盘，保证数据落盘（例如每10组调用一次）
        # self.file.flush()

    def close(self):
        self.file.close()
```

使用示例：

```python
writer = MatrixCsvWriter('matrices.csv')
while capturing:
    # 获取 A, B, C 三个4x4矩阵
    writer.write(A, B, C)
writer.close()
```

生成的文件内容示例（三个矩阵为一组）：

```
# 每组由三个4x4矩阵组成，矩阵间用空行分隔
1.000000,2.000000,3.000000,4.000000
5.000000,6.000000,7.000000,8.000000
9.000000,10.000000,11.000000,12.000000
13.000000,14.000000,15.000000,16.000000

17.000000,18.000000,19.000000,20.000000
21.000000,22.000000,23.000000,24.000000
25.000000,26.000000,27.000000,28.000000
29.000000,30.000000,31.000000,32.000000

33.000000,34.000000,35.000000,36.000000
37.000000,38.000000,39.000000,40.000000
41.000000,42.000000,43.000000,44.000000
45.000000,46.000000,47.000000,48.000000

(下一组重复：矩阵1\n矩阵2\n矩阵3\n\n...)
```

---

📖 读取方法

由于使用了空行作为组间分隔，读取时需要按块解析：

```python
import numpy as np

def read_matrix_groups(filepath):
    """
    读取文件，返回列表，每个元素为 [mat1, mat2, mat3]（均为4x4 numpy数组）
    """
    with open(filepath, 'r') as f:
        lines = [line.strip() for line in f if line.strip() and not line.startswith('#')]
        # lines 现在只包含非空、非注释的行

    # 每4行构成一个矩阵，每3个矩阵为一组
    if len(lines) % 12 != 0:
        print("警告：文件行数不是12的倍数，可能数据不完整")

    groups = []
    for i in range(0, len(lines), 12):
        # 取出该组的12行
        group_lines = lines[i:i+12]
        mats = []
        for j in range(0, 12, 4):
            mat_block = group_lines[j:j+4]
            # 将文本行转为浮点数矩阵
            mat = np.array([list(map(float, row.split(','))) for row in mat_block])
            mats.append(mat)
        groups.append(mats)   # mats = [mat1, mat2, mat3]
    return groups

# 使用
groups = read_matrix_groups('matrices.csv')
print(f"共 {len(groups)} 组数据")
print("第一组的第二个矩阵：\n", groups[0][1])
```

更健壮的解析（处理可能缺失末尾空行）：

```python
def read_matrix_groups_robust(filepath):
    groups = []
    with open(filepath, 'r') as f:
        matrix_lines = []
        for line in f:
            line = line.strip()
            if not line or line.startswith('#'):
                continue
            matrix_lines.append(line)
            # 每收集4行就组成一个矩阵
            if len(matrix_lines) == 4:
                mat = np.loadtxt(matrix_lines, delimiter=',')
                groups.append(mat)
                matrix_lines = []
    # 如果最后不足4行，丢弃并警告
    if matrix_lines:
        print(f"警告：最后 {len(matrix_lines)} 行不完整，已丢弃")
    # 将每3个矩阵打包为一组
    return [groups[i:i+3] for i in range(0, len(groups), 3)]
```

---

✅ 优点

· 人类可读：可直接用文本编辑器或 Excel 打开查看矩阵值。
· 直观：文件内容即矩阵形状，无需额外转换。
· 易于调试：出现问题时可快速检查某组矩阵是否正确。

⚠️ 缺点

· 文件体积大：相比二进制方案，文本格式膨胀约 3~5 倍。
· 解析稍慢：读取时需要按空行分割并解析文本。
· 实时写入性能：若每帧都调用 np.savetxt 并写空行，建议与之前方案一样增加内存缓冲批量写入（累积多组后一次性写入），避免频繁 I/O 降低采集帧率。

---

🚀 高性能版（带缓冲）

若帧率较高，可参照之前 CsvMatrixRecorder 的缓冲思路，累积多组矩阵后批量写入：

```python
class BufferedMatrixCsvWriter:
    def __init__(self, filepath, batch_size=10):
        self.file = open(filepath, 'w')
        self.batch_size = batch_size
        self.buffer = []
        self.file.write("# 每组三个4x4矩阵，空行分隔\n")

    def write(self, m1, m2, m3):
        # 将三个矩阵转为字符串块暂存
        block = []
        for mat in (m1, m2, m3):
            block.append('\n'.join(','.join(f'{x:.6f}' for x in row) for row in mat))
        self.buffer.append('\n\n'.join(block) + '\n\n')  # 组间加双空行
        if len(self.buffer) >= self.batch_size:
            self.flush()

    def flush(self):
        if self.buffer:
            self.file.write(''.join(self.buffer))
            self.file.flush()
            self.buffer.clear()

    def close(self):
        self.flush()
        self.file.close()
```

---

总结

· 保持矩阵外观的 CSV 方案：每个矩阵写为4行CSV，三个矩阵连续写，组间用空行分隔。
· 读取：按空行或固定行数（12行一组）解析，使用 np.loadtxt 或手动分割。
· 适用场景：数据量较小、需要人工检查或与外部工具交互时。
· 性能注意：建议使用缓冲批量写入，避免实时采集时频繁打开/关闭文件或每帧都执行文件 I/O。

如果你需要更简单、高性能且不关心可读性，请优先选择前一个答案中的二进制方案。